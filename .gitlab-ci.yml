image: maven:3.9.6-eclipse-temurin-17

stages:
  - build

cache:
  paths:
    - .m2/repository

build-war:
  stage: build

  script:
    - mvn clean package -DskipTests

  artifacts:
    paths:
      - target/*.war

  only:
    - main
    - dev